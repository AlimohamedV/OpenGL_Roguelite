# ═══════════════════════════════════════════
#  ENEMY CONFIGURATION
#  Edit these values to balance the game
#  Changes take effect on next room load
# ═══════════════════════════════════════════

# ── Chaser (red demon) ──
chaser.base_health = 2.0
chaser.health_per_depth = 0.3
chaser.radius = 0.25
chaser.base_speed = 2.2
chaser.speed_per_depth = 0.08
chaser.base_damage = 10.0
chaser.damage_per_depth = 0.5

# ── Shooter (purple orb) ──
shooter.base_health = 2.0
shooter.health_per_depth = 0.2
shooter.radius = 0.25
shooter.speed = 1.2
shooter.damage = 12.0
shooter.shoot_interval = 2.0
shooter.preferred_distance = 5.0

# ── Tank (orange golem) ──
tank.base_health = 5.0
tank.health_per_depth = 0.5
tank.radius = 0.35
tank.speed = 0.8
tank.base_damage = 15.0
tank.damage_per_depth = 1.0

# ── Boss (large demon) ──
boss.base_health = 15.0
boss.health_per_depth = 3.0
boss.radius = 0.45
boss.speed = 1.5
boss.damage = 20.0
boss.shoot_interval = 2.0
boss.charge_interval = 5.0

# ── Spawn counts ──
spawn.base_chasers = 4
spawn.chasers_per_depth = 1
spawn.shooter_start_depth = 1
spawn.base_shooters = 1
spawn.shooters_per_depth_div = 2
spawn.tank_start_depth = 3
spawn.base_tanks = 1
spawn.tanks_per_depth_div = 3
spawn.boss_extras_per_depth_div = 5
spawn.boss_max_extras = 4
